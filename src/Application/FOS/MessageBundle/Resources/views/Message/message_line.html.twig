{%if message.sender.id == app.user.id %}
    <li class="clearfix">
        <div class="message-data align-right">
            <span class="message-data-time" >{{utils.sinceOrDateTime(message.createdAt,app.request.locale, true)}}</span> &nbsp; &nbsp;
            <span class="message-data-name" >{{ message.sender.firstnameForMessage }}</span> <i class="fa fa-circle me"></i>

        </div>
        <div class="message other-message float-right">
            {{ message.body|nl2br }}
        </div>
    </li>
{%else%}
    <li>
        <div class="message-data">
            <span class="message-data-name"><i class="fa fa-circle online"></i> {{ message.sender.firstnameForMessage }}</span>
            <span class="message-data-time">{{utils.sinceOrDateTime(message.createdAt,app.request.locale, true)}}</span>
        </div>
        <div class="message my-message">
            {{ message.body|nl2br }}
        </div>
    </li>
{%endif%}




